import { StreetSection, DepartmentSection } from './index';

const config = {
    1: {
        renderSection: DepartmentSection
    },
    2: {
        renderSection: StreetSection
    }
};

export default config;
