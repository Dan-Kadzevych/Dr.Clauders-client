import React from 'react';
import styled from 'styled-components';

const Container = styled.main`
    grid-column: center-start / center-end;
`;

const ProductsGrid = () => <Container>Hello</Container>;

export default ProductsGrid;
