import styled from 'styled-components';
import { color_red, font_quaternary } from 'styles/variables';

const ErrorMessage = styled.span`
    color: ${color_red};
    ${font_quaternary};
`;

export default ErrorMessage;
