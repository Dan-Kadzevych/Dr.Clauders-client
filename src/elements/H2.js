import styled from 'styled-components';

import { color_primary } from 'styles/variables';

const H2 = styled.h2`
    font-size: 3.8rem;
    line-height: 4.9rem;
    font-weight: 300;
    color: ${color_primary};
`;

export default H2;
