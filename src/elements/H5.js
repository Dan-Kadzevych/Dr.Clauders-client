import styled from 'styled-components';

import { color_primary } from 'styles/variables';

const H5 = styled.h5`
    font-size: 2rem;
    line-height: 3rem;
    font-weight: 300;
    color: ${color_primary};
`;

export default H5;
