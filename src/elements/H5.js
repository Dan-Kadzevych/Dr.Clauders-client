import styled from 'styled-components';

import { color_primary, font_tertiary } from 'styles/variables';

const H5 = styled.h5`
    font-size: 2rem;
    line-height: 3rem;
    font-weight: 300;
    color: ${color_primary};
    ${font_tertiary};
`;

export default H5;
